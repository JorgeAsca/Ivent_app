apiVersion: v1
kind: Service
metadata:
  name: ms-gateway
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.global.env.PORT }}
      protocol: TCP
      name: http
  selector:
    app: ms-gateway