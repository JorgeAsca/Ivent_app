global:
  namespace: "inventario-app"
  nats:
    url: "nats://nats:4222"
  env:
    PORT: 3000
  # Datos sensibles (en un entorno real usarías un archivo externo o secret management)
  database:
    postgres:
      user: "jorge_admin"
      pass: "ivent_secure_pass_2024"
      dbName: "inventario_db"
      host: "postgres-service"
  jwt:
    seed: "mi_semilla_secreta_para_tokens"

# Activación de servicios
infra-nats:
  enabled: true

ms-gateway:
  enabled: true